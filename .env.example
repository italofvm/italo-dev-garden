# URL base da API (ex: https://api.seusite.com)
VITE_API_URL=

# Chave secreta usada para assinar tokens JWT no servidor
# Gere com: openssl rand -base64 32
JWT_SECRET=

# Ambiente de execução
NODE_ENV=development
